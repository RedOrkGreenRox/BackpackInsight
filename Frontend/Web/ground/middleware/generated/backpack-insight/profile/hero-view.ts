// automatically generated by the FlatBuffers compiler, do not modify

import * as flatbuffers from 'flatbuffers';

export class HeroView {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):HeroView {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsHeroView(bb:flatbuffers.ByteBuffer, obj?:HeroView):HeroView {
  return (obj || new HeroView()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsHeroView(bb:flatbuffers.ByteBuffer, obj?:HeroView):HeroView {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new HeroView()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

name():string|null
name(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
name(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

level():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

rating():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

experience():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

expReq():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

prestige():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

league():string|null
league(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
league(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

skinNum():string|null
skinNum(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
skinNum(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

static startHeroView(builder:flatbuffers.Builder) {
  builder.startObject(8);
}

static addName(builder:flatbuffers.Builder, nameOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, nameOffset, 0);
}

static addLevel(builder:flatbuffers.Builder, level:number) {
  builder.addFieldInt32(1, level, 0);
}

static addRating(builder:flatbuffers.Builder, rating:number) {
  builder.addFieldInt32(2, rating, 0);
}

static addExperience(builder:flatbuffers.Builder, experience:bigint) {
  builder.addFieldInt64(3, experience, BigInt('0'));
}

static addExpReq(builder:flatbuffers.Builder, expReq:bigint) {
  builder.addFieldInt64(4, expReq, BigInt('0'));
}

static addPrestige(builder:flatbuffers.Builder, prestige:boolean) {
  builder.addFieldInt8(5, +prestige, +false);
}

static addLeague(builder:flatbuffers.Builder, leagueOffset:flatbuffers.Offset) {
  builder.addFieldOffset(6, leagueOffset, 0);
}

static addSkinNum(builder:flatbuffers.Builder, skinNumOffset:flatbuffers.Offset) {
  builder.addFieldOffset(7, skinNumOffset, 0);
}

static endHeroView(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  builder.requiredField(offset, 4) // name
  return offset;
}

static createHeroView(builder:flatbuffers.Builder, nameOffset:flatbuffers.Offset, level:number, rating:number, experience:bigint, expReq:bigint, prestige:boolean, leagueOffset:flatbuffers.Offset, skinNumOffset:flatbuffers.Offset):flatbuffers.Offset {
  HeroView.startHeroView(builder);
  HeroView.addName(builder, nameOffset);
  HeroView.addLevel(builder, level);
  HeroView.addRating(builder, rating);
  HeroView.addExperience(builder, experience);
  HeroView.addExpReq(builder, expReq);
  HeroView.addPrestige(builder, prestige);
  HeroView.addLeague(builder, leagueOffset);
  HeroView.addSkinNum(builder, skinNumOffset);
  return HeroView.endHeroView(builder);
}
}
