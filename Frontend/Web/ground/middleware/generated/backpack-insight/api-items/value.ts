// automatically generated by the FlatBuffers compiler, do not modify

import * as flatbuffers from 'flatbuffers';

import { KeyValue } from '../../backpack-insight/api-items/key-value.js';
import { ValueKind } from '../../backpack-insight/api-items/value-kind.js';


export class Value {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):Value {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsValue(bb:flatbuffers.ByteBuffer, obj?:Value):Value {
  return (obj || new Value()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsValue(bb:flatbuffers.ByteBuffer, obj?:Value):Value {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new Value()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

kind():ValueKind {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : ValueKind.Null;
}

boolValue():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

intValue():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readInt64(this.bb_pos + offset) : BigInt('0');
}

floatValue():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

stringValue():string|null
stringValue(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
stringValue(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

arrayValue(index: number, obj?:Value):Value|null {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? (obj || new Value()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

arrayValueLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

objectValue(index: number, obj?:KeyValue):KeyValue|null {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? (obj || new KeyValue()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

objectValueLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

static startValue(builder:flatbuffers.Builder) {
  builder.startObject(7);
}

static addKind(builder:flatbuffers.Builder, kind:ValueKind) {
  builder.addFieldInt8(0, kind, ValueKind.Null);
}

static addBoolValue(builder:flatbuffers.Builder, boolValue:boolean) {
  builder.addFieldInt8(1, +boolValue, +false);
}

static addIntValue(builder:flatbuffers.Builder, intValue:bigint) {
  builder.addFieldInt64(2, intValue, BigInt('0'));
}

static addFloatValue(builder:flatbuffers.Builder, floatValue:number) {
  builder.addFieldFloat64(3, floatValue, 0.0);
}

static addStringValue(builder:flatbuffers.Builder, stringValueOffset:flatbuffers.Offset) {
  builder.addFieldOffset(4, stringValueOffset, 0);
}

static addArrayValue(builder:flatbuffers.Builder, arrayValueOffset:flatbuffers.Offset) {
  builder.addFieldOffset(5, arrayValueOffset, 0);
}

static createArrayValueVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startArrayValueVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addObjectValue(builder:flatbuffers.Builder, objectValueOffset:flatbuffers.Offset) {
  builder.addFieldOffset(6, objectValueOffset, 0);
}

static createObjectValueVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startObjectValueVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static endValue(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createValue(builder:flatbuffers.Builder, kind:ValueKind, boolValue:boolean, intValue:bigint, floatValue:number, stringValueOffset:flatbuffers.Offset, arrayValueOffset:flatbuffers.Offset, objectValueOffset:flatbuffers.Offset):flatbuffers.Offset {
  Value.startValue(builder);
  Value.addKind(builder, kind);
  Value.addBoolValue(builder, boolValue);
  Value.addIntValue(builder, intValue);
  Value.addFloatValue(builder, floatValue);
  Value.addStringValue(builder, stringValueOffset);
  Value.addArrayValue(builder, arrayValueOffset);
  Value.addObjectValue(builder, objectValueOffset);
  return Value.endValue(builder);
}
}
