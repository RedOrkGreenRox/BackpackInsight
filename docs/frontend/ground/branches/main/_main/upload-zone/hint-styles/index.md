# [Barrel стилей подсказки (index.scss)](../../../../../../../../Frontend/Web/ground/branches/main/_main/upload-zone/hint-styles/index.scss)

## Назначение
Агрегатор стилей подсказки зоны загрузки. Подключает части через `@use`.

## Подключения
*   `@use "upload-hint-base"` — [база подсказки](upload-hint-base.md).
*   `@use "upload-hint-pc-only"` — [часть только для ПК](upload-hint-pc-only.md).

## AI-контекст
*   Barrel-файл. Логику видимости/адаптива держите в подключаемых атомах.

---

> 📌 **Подпись документации:** создано при рефактор-документировании (приоритет по глубине вложенности).
