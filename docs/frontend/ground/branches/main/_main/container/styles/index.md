# [Стили контейнера главной (index.scss)](../../../../../../../../Frontend/Web/ground/branches/main/_main/container/styles/index.scss)

## Назначение
Файл-агрегатор стилей контейнера главной страницы. Сам правил не содержит — только подключает части через `@use`.

## Задаваемые стили / подключения
*   `@use "container-base"` — базовая геометрия/фон контейнера.
*   `@use "container-responsive"` — адаптивные переопределения для разных экранов.

## AI-контекст
*   Это barrel-файл стилей. Порядок `@use` определяет каскад: базовые правила раньше, адаптивные переопределения позже.

---

> 📌 **Подпись документации:** актуализировано при аудите сетевой документации (добавлен детальный разбор стилей).
