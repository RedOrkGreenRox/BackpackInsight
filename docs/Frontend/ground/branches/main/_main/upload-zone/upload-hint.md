# [Стили подсказки — агрегатор (upload-hint.scss)](../../../../../../../Frontend/Web/ground/branches/main/_main/upload-zone/upload-hint.scss)

## Назначение
Агрегатор: подключает модульные стили подсказки `.upload-hint` из `hint-styles/`.

## Подключения
*   `@use "hint-styles"` → [hint-styles/index](hint-styles/index.md) (база + pc-only).

## AI-контекст
*   Реальные правила — в `hint-styles/*`.

---

> 📌 **Подпись документации:** создано при рефактор-документировании (приоритет по глубине; `.ts`+`.scss` одного компонента объединены).
