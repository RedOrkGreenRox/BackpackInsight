# [Стили области загрузки — агрегатор (upload-area.scss)](../../../../../../../Frontend/Web/ground/branches/main/_main/upload-zone/upload-area.scss)

## Назначение
Агрегатор: подключает модульные стили `.upload-area` из `styles/`.

## Подключения
*   `@use "styles"` → [styles/index](styles/index.md) (база/hover/выделение/textarea/адаптив).

## AI-контекст
*   Реальные правила — в `styles/*`.

---

> 📌 **Подпись документации:** создано при рефактор-документировании (приоритет по глубине; `.ts`+`.scss` одного компонента объединены).
