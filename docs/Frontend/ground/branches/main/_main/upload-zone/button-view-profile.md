# [Стили кнопки профиля — агрегатор (button-view-profile.scss)](../../../../../../../Frontend/Web/ground/branches/main/_main/upload-zone/button-view-profile.scss)

## Назначение
Тонкий агрегатор: подключает модульные стили кнопки `.button-view-profile` из `button-styles/`.

## Подключения
*   `@use "button-styles"` → [button-styles/index](button-styles/index.md) (база/состояния/адаптив).

## AI-контекст
*   Реальные правила — в `button-styles/*`. Этот файл существует для исторического имени импорта.

---

> 📌 **Подпись документации:** создано при рефактор-документировании (приоритет по глубине; `.ts`+`.scss` одного компонента объединены).
