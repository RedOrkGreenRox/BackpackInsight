# [pytest.ini для scripts (pytest.ini)](../../scripts/pytest.ini)

## Назначение
Локальный конфиг pytest в каталоге `scripts/` (вариант с `-v` выводом).

## Содержимое
*   `testpaths=tests`, шаблоны имён, `addopts=-v --tb=short`.

## AI-контекст
*   Дублирует основной [pytest.ini](../pytest.md) с подробным выводом. Основной конфиг — корневой.

---

> 📌 **Подпись документации:** создано при добивании полного покрытия (все файлы, включая конфиги/данные/PWA).
