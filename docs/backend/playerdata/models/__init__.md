# [Маркер пакета (__init__.py)](../../../../Backend/PlayerData/models/__init__.py)

## Назначение
Пакет моделей. Реэкспортирует Profile/Hero/Item/ItemDefinition для импорта `from ...models import ...` и регистрации в SQLModel.metadata.

## Содержимое
*   Файл пустой (или почти пустой) — служит маркером Python-пакета.

## AI-контекст
*   Технический файл пакетной системы Python (`Backend.PlayerData.models`). Логики не несёт; нужен для корректных импортов. Особо важен для `models/__init__.py` — через него регистрируются таблицы в метаданных (используется Alembic и тестами).

---

> 📌 **Подпись документации:** создано при добивании полного покрытия (все файлы, включая конфиги/данные/PWA).
