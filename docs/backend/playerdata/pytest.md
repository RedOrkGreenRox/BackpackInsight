# [pytest.ini для PlayerData (pytest.ini)](../../../Backend/PlayerData/pytest.ini)

## Назначение
Минимальный локальный конфиг pytest в `Backend/PlayerData/`.

## Содержимое
*   `addopts=-s -ra -W ignore` (показывать print, игнорировать предупреждения).

## AI-контекст
*   Вспомогательный. Канонический набор тестов и конфиг — в корне ([pytest.ini](../../pytest.md)).

---

> 📌 **Подпись документации:** создано при добивании полного покрытия (все файлы, включая конфиги/данные/PWA).
