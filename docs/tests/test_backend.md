# [Тест бэкенда (test_backend.py)](../../tests/test_backend.py)

## Назначение
Интеграционные проверки бэкенда (загрузка статики, поиск предметов).

## ⚠️ Статус: НЕРАБОЧИЙ (мёртвый импорт)
Импортирует `from Backend.PlayerData.services.Search import search_items` — модуля `services/Search.py` в проекте **нет**. Тест падает на стадии сбора pytest и блокирует весь прогон, пока не исправлен/исключён.

## AI-контекст
*   **Требует действия по идеологии**: либо восстановить функциональность поиска (`services/Search`), либо переписать/удалить тест, и только затем привести док в соответствие. Сейчас весь набор тестов нельзя запустить без `--ignore` этого файла.

---

> 📌 **Подпись документации:** создано при рефактор-документировании (приоритет по глубине вложенности).
