# [Маркер пакета (__init__.py)](../../../../Backend/PlayerData/Profiles/__init__.py)

## Назначение
Пакет каталога профилей-образцов.

## Содержимое
*   Файл пустой (или почти пустой) — служит маркером Python-пакета.

## AI-контекст
*   Технический файл пакетной системы Python (`Backend.PlayerData.Profiles`). Логики не несёт; нужен для корректных импортов. Особо важен для `models/__init__.py` — через него регистрируются таблицы в метаданных (используется Alembic и тестами).

---

> 📌 **Подпись документации:** создано при добивании полного покрытия (все файлы, включая конфиги/данные/PWA).
