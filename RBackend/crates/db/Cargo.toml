[package]
name = "db"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
middleware = { path = "../middleware" }
sqlx = { version = "0.8", default-features = false, features = ["runtime-tokio", "tls-rustls", "postgres", "sqlite", "any", "migrate", "macros"] }
libsqlite3-sys = { version = "0.30", features = ["bundled"] }

[lints]
workspace = true
