[package]
name = "builder"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
rbackend_core = { path = "../core" }
pack = { path = "../pack" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lints]
workspace = true
